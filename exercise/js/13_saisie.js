var nb=0;//initialiser la variable nombre a 0 pour declencher l'exécution de la boucle
while((nb<50) || (nb>100)){
   nb=parseInt(prompt("tappez un chiffre entre 50 et 100"));
}
// inverser
var nb1=prompt("tappez un chiffre");//initialiser la variable nombre a 0 pour declencher l'exécution de la boucle
while(!((nb1<50) || (nb1>100))){
   nb1=parseInt(prompt("tappez un chiffre superieur 50 et 100"));
}